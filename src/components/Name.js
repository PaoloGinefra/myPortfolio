import React from "react";

const Name = ({textColor}) => {
    return <h2 className={`text-center text-5xl py-2 ${textColor} font-medium md:text-6xl`}>
        Paolo Ginefra
    </h2>
}

export default Name;